<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.btitsolutions.cookiecatcher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.btitsolutions.cookiecatcher.StarterActivity"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.btitsolutions.cookiecatcher.ResultActivity"
            android:label="@string/app_title"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.btitsolutions.cookiecatcher.MainActivity"
            android:label="@string/app_title"
            android:screenOrientation="fullSensor" />
        <activity android:name="com.btitsolutions.cookiecatcher.SettingActivity"
            android:label="@string/app_title" />
    </application>

</manifest>